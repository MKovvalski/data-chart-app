import React from "react";

const Header: React.FC = () => {
    return (
        <div className="header">
            <header>Data charts app</header>
        </div>
    );
};

export default Header;
