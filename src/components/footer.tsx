import React from "react";

const Footer: React.FC = () => {
    return (
        <div className="footer">
            <footer>Welcome the footer</footer>
        </div>
    );
};

export default Footer;
